package SlidingWindow;

//Transform and Sort: Convert the two-array comparison into a single array $diff[i] = nums1[i] - nums2[i]$.
// The problem then becomes finding pairs where $diff[i] + diff[j] > 0$. Sort the array and use
// Two Pointers: if the ends sum to $> 0$, then the right element pairs with everyone in between—count
// them and move right in. Otherwise, move left up.


public class CountPairsInTwoArrays {
    static void main() {

    }
}
