package SlidingWindow;


//The logic changes slightly: instead of minimizing, we use the property that if a window [left, right]
// contains all three characters ('a', 'b', and 'c'), then any substring starting at an index before left
// and ending at right also contains all three.

public class NumberOfSubstringsContainingAllThreeCharacters {
}
